{
  "name": "bridge",
  "private": true,
  "version": "3.14.0",
  "description": "Bridge — shift checklists, tickets and CRM (Shiftly)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
